{
  "name": "monorepo-root",
  "private": true,
  "scripts": {
    "release": "changeset publish",
    "build": "pnpm --filter '*' build",
    "lint": "pnpm --filter '*' lint"
  },
  "pnpm": {
    "overrides": {
      "@types/eslint": "^8.0.0"
    }
  },
  "volta": {
    "node": "18.12.1",
    "pnpm": "8.6.5"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.0"
  }
}
